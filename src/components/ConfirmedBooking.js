import { BrowserRouter as Router, Routes, Route } from "react-router-dom";

function ConfirmedBooking() {
  return (
    <div className="confirmedBody">
      <h1 className="confirmed">Booking Confirmed!</h1>
    </div>
  );
}

export default ConfirmedBooking;